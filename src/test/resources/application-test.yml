spring:
  mail:
    host: localhost
    port: 3025
    test-connection: false

jwt:
  secret: test-secret-key-for-ci-at-least-32-chars!!

wisewallet:
  internal:
    jwt:
      secret: test-internal-jwt-secret-for-ci-min32!!
    api-key: test-internal-api-key
